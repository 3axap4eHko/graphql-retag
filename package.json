{
  "name": "graphql-retag",
  "version": "0.1.3",
  "description": "Helpful utility that wraps `graphql-tag` module for parsing GraphQL queries, supports nesting fragments and resolves dependencies.",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf build && tsc --declaration --outDir build && cp package.json README.md build",
    "test": "jest",
    "release": "git pull --rebase && npm version patch && npm run build && npm publish build && git push --tags && git push"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/3axap4eHko/graphql-retag.git"
  },
  "keywords": [],
  "author": "Ivan Zakharchanka",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/3axap4eHko/graphql-retag/issues"
  },
  "homepage": "https://github.com/3axap4eHko/graphql-retag#readme",
  "devDependencies": {
    "@types/graphql": "^14.0.5",
    "@types/jest": "^23.3.13",
    "graphql": "^14.1.1",
    "graphql-tag": "^2.10.1",
    "jest": "^24.0.0",
    "ts-jest": "^23.10.5",
    "typescript": "^3.2.4"
  },
  "peerDependencies": {
    "graphql-tag": "^2.10.1"
  }
}
